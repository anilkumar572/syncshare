{
  "name": "sharesyncapp",
  "private": true,
  "scripts": {
    "deploy": "bash scripts/deploy.sh hosting",
    "deploy:all": "bash scripts/deploy.sh hosting,firestore:rules",
    "deploy:rules": "bash scripts/deploy.sh firestore:rules",
    "deploy:hosting": "bash scripts/deploy.sh hosting",
    "firebase:login": "firebase login",
    "firebase:login:ci": "firebase login:ci"
  },
  "devDependencies": {
    "firebase-tools": "^13.35.1"
  }
}
